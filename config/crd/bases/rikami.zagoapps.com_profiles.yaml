---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: profiles.rikami.zagoapps.com
spec:
  group: rikami.zagoapps.com
  names:
    kind: Profile
    listKind: ProfileList
    plural: profiles
    singular: profile
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Profile is the Schema for the profiles API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Profile
            properties:
              databaseConfig:
                description: DatabaseConfig determines default parameters to create
                  external secret containing pg url login for atlas migration/schema
                  role
                properties:
                  appConfig:
                    description: DatabaseAppConfig configures creation of externalsecret
                      that would be injected as env into pods for app access to db
                    properties:
                      hostKey:
                        type: string
                      passwordKey:
                        type: string
                      secretAppPath:
                        type: string
                      usernameKey:
                        type: string
                    required:
                    - hostKey
                    - passwordKey
                    - secretAppPath
                    - usernameKey
                    type: object
                  envKeysPrefix:
                    default: db_
                    type: string
                  migratorConfig:
                    description: DatabaseMigratorConfig configures creation of externalsecret
                      that would be used by atlasschema to get the pg url
                    properties:
                      secretKey:
                        type: string
                      secretMigratorPath:
                        type: string
                    required:
                    - secretKey
                    - secretMigratorPath
                    type: object
                required:
                - appConfig
                - envKeysPrefix
                - migratorConfig
                type: object
              domain:
                minLength: 4
                pattern: '[A-Za-z0-9]+\.[A-Za-z]+'
                type: string
              externalSecretsConfig:
                description: ExternalSecretsConfig determines default configuration
                  for ExternalSecrets resources
                properties:
                  refreshInterval:
                    type: string
                  refreshPolicy:
                    description: ExternalSecretRefreshPolicy defines how and when
                      the ExternalSecret should be refreshed.
                    enum:
                    - CreatedOnce
                    - Periodic
                    - OnChange
                    type: string
                  secretStoreRef:
                    description: SecretStoreRef defines which SecretStore to fetch
                      the ExternalSecret data.
                    properties:
                      kind:
                        description: |-
                          Kind of the SecretStore resource (SecretStore or ClusterSecretStore)
                          Defaults to `SecretStore`
                        enum:
                        - SecretStore
                        - ClusterSecretStore
                        type: string
                      name:
                        description: Name of the SecretStore resource
                        maxLength: 253
                        minLength: 1
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                        type: string
                    type: object
                required:
                - refreshInterval
                - refreshPolicy
                - secretStoreRef
                type: object
              gateway:
                minLength: 1
                type: string
              namespacedSubdomain:
                default: false
                type: boolean
              pullPolicy:
                description: PullPolicy describes a policy for if/when to pull a container
                  image
                enum:
                - Always
                - IfNotPresent
                - Never
                type: string
              pullSecret:
                minLength: 1
                type: string
            required:
            - databaseConfig
            - domain
            - externalSecretsConfig
            - gateway
            - namespacedSubdomain
            - pullPolicy
            type: object
          status:
            description: status defines the observed state of Profile
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the Profile resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
